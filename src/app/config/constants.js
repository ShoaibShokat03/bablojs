import { heroStats, communityStats } from "../../constants/stats.js";
import { followUsLinks } from "../../constants/links.js";

const constants = {
    heroStats: heroStats,
    communityStats: communityStats,
    followUsLinks: followUsLinks,
}

export default constants;

